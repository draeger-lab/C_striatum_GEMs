name: FROG analysis of all models

on:
  workflow_dispatch
  
jobs:
  run:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      with:
       persist-credentials: false
       fetch-depth: 0
       
    - name: Set up Python
      uses: actions/setup-python@v1
      with:
        python-version: '3.9'
        
    - name: Install dependencies
      run: python -m pip install markupsafe==2.0.1

    - name: Install dependencies
      run: python -m pip install fbc-curation
        
    - name: Commit FROG report
      run: |
        runfrog -i ./models/Cstr_14.xml -o ./FROG/Cstr_14
        runfrog -i ./models/Cstr_15.xml -o ./FROG/Cstr_15
        runfrog -i ./models/Cstr_16.xml -o ./FROG/Cstr_16
        runfrog -i ./models/Cstr_17.xml -o ./FROG/Cstr_17
        runfrog -i ./models/Cstr_KC-Na-01.xml -o ./FROG/Cstr_KC-Na-01

    - name: Commit FROG report
      id: commit
      run: |
        git config --local user.email "famke.baeuerle@gmail.com"
        git config --local user.name "famosab"
        git add --all
        if [-z "$(git status --porcelain)"]; then
           echo "::set-output name=push::false"
        else
           git commit -m "Update FROG Report" -a
           echo "::set-output name=push::true"
        fi
 
    - name: Push changes of the report
      if: steps.commit.outputs.push == 'true'
      uses: ad-m/github-push-action@master
      with:
         github_token: ${{ secrets.GITHUB_TOKEN }}
